let rawRows = [], currentCountyId = null, currentMetric = 'AnzahlFall', dateValues = []; let countyIndex = {};
async function loadCSV(path = '../Bereinigte_Daten_2026_06_11.csv') { return new Promise(r => Papa.parse(path, { download: true, header: true, dynamicTyping: true, complete: x => { rawRows = x.data.filter(d => d.IdLandkreis); rawRows.forEach(row => { const id = String(row.IdLandkreis); (countyIndex[id] ??= []).push(row); }); dateValues = [...new Set(rawRows.map(d => d.Refdatum))].sort(); r(); } })); }